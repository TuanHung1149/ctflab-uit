server {

    listen 80;
    listen [::]:80;

    server_name inffile123.infinity.insec;

    location / {
        proxy_pass http://file-manager;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $host;
        proxy_redirect off;
    }
}